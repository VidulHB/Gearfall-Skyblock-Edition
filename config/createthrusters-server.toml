#Authoritative server-side behavior
[server]
	#Spawn thruster exhaust particles while active
	enableThrusterParticles = true
	#Multiplier for active thruster particle count
	# Default: 1.0
	# Range: 0.0 ~ 4.0
	thrusterParticleIntensity = 1.0
	#Force thruster air streams to bulk-process items using Create fan blasting or haunting
	enableThrusterBulkProcessing = true
	#Bulk processing rate multiplier at 100% throttle, relative to Create's default fan processing speed. Processing scales linearly with throttle, so 50% throttle matches Create when this is 2.0
	# Default: 2.0
	# Range: 0.0 ~ 64.0
	thrusterBulkProcessingFullThrottleMultiplier = 2.0
	#Allow linking Advanced Data Link blocks to Simulated gyro sensors
	enableGyroscopeLinking = true
	#Maximum block distance between selected gyro sensor and placed Advanced Data Link, matching Create's Display Link flow
	# Default: 128
	# Range: 1 ~ 1024
	gyroscopeLinkRange = 128
	#How many ticks a virtual orientation source remains active without an update
	# Default: 40
	# Range: 1 ~ 1200
	virtualOrientationSourceTimeoutTicks = 40
	#Maximum tilt magnitude (degrees) when deriving orientation from Analogue Contraption Controller pitch/roll axes
	# Default: 45.0
	# Range: 1.0 ~ 89.0
	controllerOrientationSourceMaxTiltDegrees = 45.0
	#Allow Use-key toggling of the enabled state on thrusters
	allowThrusterManualToggle = true
	#Maximum allowed link distance for Physics Gantry Belt Wheel (hard capped to 64 blocks)
	# Default: 64
	# Range: 1 ~ 64
	physicsGantryBeltWheelMaxDistance = 64

	#Server-owned content availability. Clients use these values while connected.
	[server.features]

		[server.features.blocks]
			#Allow thruster on this server. Disabled content is removed when loaded.
			thruster = true
			#Allow fuel oxidizer on this server. Disabled content is removed when loaded.
			fuel_oxidizer = true
			#Allow thruster bearing on this server. Disabled content is removed when loaded.
			thruster_bearing = true
			#Allow thruster bearing link on this server. Disabled content is removed when loaded.
			thruster_bearing_link = true
			#Allow aileron bearing on this server. Disabled content is removed when loaded.
			aileron_bearing = true
			#Allow aileron bearing link on this server. Disabled content is removed when loaded.
			aileron_bearing_link = true
			#Allow vector bearing on this server. Disabled content is removed when loaded.
			vector_bearing = true
			#Allow vector bearing link on this server. Disabled content is removed when loaded.
			vector_bearing_link = true
			#Allow scissor piston on this server. Disabled content is removed when loaded.
			scissor_piston = true
			#Allow scissor piston link on this server. Disabled content is removed when loaded.
			scissor_piston_link = true
			#Allow scissor piston arm on this server. Disabled content is removed when loaded.
			scissor_piston_arm = true
			#Allow gyroscope link on this server. Disabled content is removed when loaded.
			gyroscope_link = true
			#Allow double button on this server. Disabled content is removed when loaded.
			double_button = true
			#Allow copycat double button on this server. Disabled content is removed when loaded.
			copycat_double_button = true
			#Allow virtual orientation source on this server. Disabled content is removed when loaded.
			virtual_orientation_source = false
			#Allow gyro redstone bridge on this server. Disabled content is removed when loaded.
			gyro_redstone_bridge = false
			#Allow bidirectional gearbox on this server. Disabled content is removed when loaded.
			bidirectional_gearbox = true
			#Allow bi directional gearshift on this server. Disabled content is removed when loaded.
			bi_directional_gearshift = true
			#Allow analogue joystick on this server. Disabled content is removed when loaded.
			analogue_joystick = true
			#Allow analogue contraption controller on this server. Disabled content is removed when loaded.
			analogue_contraption_controller = true
			#Allow contraption network linker plane on this server. Disabled content is removed when loaded.
			contraption_network_linker_plane = true
			#Allow advanced contraption controller on this server. Disabled content is removed when loaded.
			advanced_contraption_controller = true
			#Allow advanced navigation table on this server. Disabled content is removed when loaded.
			advanced_navigation_table = true
			#Allow shipping manifest on this server. Disabled content is removed when loaded.
			shipping_manifest = true
			#Allow alternator on this server. Disabled content is removed when loaded.
			alternator = true
			#Allow claw on this server. Disabled content is removed when loaded.
			claw = true
			#Allow entity launcher anchor on this server. Disabled content is removed when loaded.
			entity_launcher_anchor = true
			#Allow powered zipline on this server. Disabled content is removed when loaded.
			powered_zipline = true
			#Allow rope knot on this server. Disabled content is removed when loaded.
			rope_knot = true
			#Allow launcher endpoint on this server. Disabled content is removed when loaded.
			launcher_endpoint = true
			#Allow andesite cable on this server. Disabled content is removed when loaded.
			andesite_cable = true
			#Allow industrial motor on this server. Disabled content is removed when loaded.
			industrial_motor = true
			#Allow variable transmission on this server. Disabled content is removed when loaded.
			variable_transmission = true
			#Allow physics gantry carriage on this server. Disabled content is removed when loaded.
			physics_gantry_carriage = true
			#Allow physics gantry shaft on this server. Disabled content is removed when loaded.
			physics_gantry_shaft = true
			#Allow physics gantry belt wheel on this server. Disabled content is removed when loaded.
			physics_gantry_belt_wheel = true

		[server.features.items]
			#Allow thruster on this server. Disabled content is removed when loaded.
			thruster = true
			#Allow small thruster on this server. Disabled content is removed when loaded.
			small_thruster = true
			#Allow fuel oxidizer on this server. Disabled content is removed when loaded.
			fuel_oxidizer = true
			#Allow thruster bearing on this server. Disabled content is removed when loaded.
			thruster_bearing = true
			#Allow aileron bearing on this server. Disabled content is removed when loaded.
			aileron_bearing = true
			#Allow vector bearing on this server. Disabled content is removed when loaded.
			vector_bearing = true
			#Allow scissor piston on this server. Disabled content is removed when loaded.
			scissor_piston = true
			#Allow scissor arms on this server. Disabled content is removed when loaded.
			scissor_arms = true
			#Allow gyroscope link on this server. Disabled content is removed when loaded.
			gyroscope_link = true
			#Allow double button on this server. Disabled content is removed when loaded.
			double_button = true
			#Allow copycat double button on this server. Disabled content is removed when loaded.
			copycat_double_button = true
			#Allow virtual orientation source on this server. Disabled content is removed when loaded.
			virtual_orientation_source = false
			#Allow gyro redstone bridge on this server. Disabled content is removed when loaded.
			gyro_redstone_bridge = false
			#Allow bidirectional gearbox on this server. Disabled content is removed when loaded.
			bidirectional_gearbox = true
			#Allow bi directional gearshift on this server. Disabled content is removed when loaded.
			bi_directional_gearshift = true
			#Allow vertical bidirectional gearbox on this server. Disabled content is removed when loaded.
			vertical_bidirectional_gearbox = true
			#Allow analogue joystick on this server. Disabled content is removed when loaded.
			analogue_joystick = true
			#Allow analogue contraption controller on this server. Disabled content is removed when loaded.
			analogue_contraption_controller = true
			#Allow advanced contraption controller on this server. Disabled content is removed when loaded.
			advanced_contraption_controller = true
			#Allow advanced navigation table on this server. Disabled content is removed when loaded.
			advanced_navigation_table = true
			#Allow portable contraption controller on this server. Disabled content is removed when loaded.
			portable_contraption_controller = true
			#Allow advanced portable contraption controller on this server. Disabled content is removed when loaded.
			advanced_portable_contraption_controller = true
			#Allow alternator on this server. Disabled content is removed when loaded.
			alternator = true
			#Allow claw on this server. Disabled content is removed when loaded.
			claw = true
			#Allow powered zipline on this server. Disabled content is removed when loaded.
			powered_zipline = true
			#Allow rope knot on this server. Disabled content is removed when loaded.
			rope_knot = true
			#Allow andesite cable on this server. Disabled content is removed when loaded.
			andesite_cable = true
			#Allow industrial motor on this server. Disabled content is removed when loaded.
			industrial_motor = true
			#Allow variable transmission on this server. Disabled content is removed when loaded.
			variable_transmission = true
			#Allow vertical variable transmission on this server. Disabled content is removed when loaded.
			vertical_variable_transmission = true
			#Allow physics gantry carriage on this server. Disabled content is removed when loaded.
			physics_gantry_carriage = true
			#Allow physics gantry shaft on this server. Disabled content is removed when loaded.
			physics_gantry_shaft = true
			#Allow physics gantry belt wheel on this server. Disabled content is removed when loaded.
			physics_gantry_belt_wheel = true
			#Allow thruster lense on this server. Disabled content is removed when loaded.
			thruster_lense = true
			#Allow processing upgrade smoking t1 on this server. Disabled content is removed when loaded.
			processing_upgrade_smoking_t1 = true
			#Allow processing upgrade smoking t2 on this server. Disabled content is removed when loaded.
			processing_upgrade_smoking_t2 = true
			#Allow processing upgrade smoking t3 on this server. Disabled content is removed when loaded.
			processing_upgrade_smoking_t3 = true
			#Allow processing upgrade smoking t4 on this server. Disabled content is removed when loaded.
			processing_upgrade_smoking_t4 = true
			#Allow processing upgrade smelting t1 on this server. Disabled content is removed when loaded.
			processing_upgrade_smelting_t1 = true
			#Allow processing upgrade smelting t2 on this server. Disabled content is removed when loaded.
			processing_upgrade_smelting_t2 = true
			#Allow processing upgrade smelting t3 on this server. Disabled content is removed when loaded.
			processing_upgrade_smelting_t3 = true
			#Allow processing upgrade smelting t4 on this server. Disabled content is removed when loaded.
			processing_upgrade_smelting_t4 = true
			#Allow processing upgrade haunting t1 on this server. Disabled content is removed when loaded.
			processing_upgrade_haunting_t1 = true
			#Allow processing upgrade haunting t2 on this server. Disabled content is removed when loaded.
			processing_upgrade_haunting_t2 = true
			#Allow processing upgrade haunting t3 on this server. Disabled content is removed when loaded.
			processing_upgrade_haunting_t3 = true
			#Allow processing upgrade haunting t4 on this server. Disabled content is removed when loaded.
			processing_upgrade_haunting_t4 = true
			#Allow propulsion upgrade t1 on this server. Disabled content is removed when loaded.
			propulsion_upgrade_t1 = true
			#Allow propulsion upgrade t2 on this server. Disabled content is removed when loaded.
			propulsion_upgrade_t2 = true
			#Allow propulsion upgrade t3 on this server. Disabled content is removed when loaded.
			propulsion_upgrade_t3 = true
			#Allow propulsion upgrade t4 on this server. Disabled content is removed when loaded.
			propulsion_upgrade_t4 = true
			#Allow physics staff on this server. Disabled content is removed when loaded.
			physics_staff = true
			#Allow contraption network linker on this server. Disabled content is removed when loaded.
			contraption_network_linker = true
			#Allow configuration clipboard on this server. Disabled content is removed when loaded.
			configuration_clipboard = true
			#Allow shipping manifest on this server. Disabled content is removed when loaded.
			shipping_manifest = true
			#Allow entity launcher on this server. Disabled content is removed when loaded.
			entity_launcher = true
			#Allow player mannequin on this server. Disabled content is removed when loaded.
			player_mannequin = true
			#Allow physics goggles on this server. Disabled content is removed when loaded.
			physics_goggles = true
			#Allow oxidized creative blaze cake on this server. Disabled content is removed when loaded.
			oxidized_creative_blaze_cake = true
			#Allow music disc kinetic currency on this server. Disabled content is removed when loaded.
			music_disc_kinetic_currency = true
			#Allow music disc twisted alive on this server. Disabled content is removed when loaded.
			music_disc_twisted_alive = true
			#Allow music disc unplug the earth on this server. Disabled content is removed when loaded.
			music_disc_unplug_the_earth = true

		[server.features.entities]
			#Allow launched claw on this server. Disabled content is removed when loaded.
			launched_claw = true
			#Allow player mannequin on this server. Disabled content is removed when loaded.
			player_mannequin = true
